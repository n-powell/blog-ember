<div class="panel">
  <div class="panel-heading">
    <h3 class="panel-title">
      {{#link-to 'post' post.id}}{{post.title}}{{/link-to}} {{post.created}}
    </h3>
  </div>
  <div class="panel-body">
    <img src={{post.img}}>
    <p>{{post.body}}</p>
  </div>
</div>
